# Restricciones por versión mínima (no pins exactos), para que pip descargue
# los wheels YA COMPILADOS disponibles para tu versión de Python — incluido
# Python 3.14 — en lugar de intentar compilar scipy/numpy desde el código fuente.
Django>=5.1,<6.1
numpy>=2.1
scipy>=1.14
openpyxl>=3.1
whitenoise>=6.6
python-decouple>=3.8
# gunicorn solo aplica a despliegues Linux (no Windows, no Vercel):
gunicorn>=22.0; sys_platform != "win32"
